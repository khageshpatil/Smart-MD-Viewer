# GitHub OAuth Configuration (Optional)
# To enable full GitHub OAuth flow, create a GitHub OAuth App and set these values
# For now, you can use Personal Access Tokens (PAT) for authentication

# VITE_GITHUB_CLIENT_ID=your_github_client_id_here
# VITE_GITHUB_REDIRECT_URI=http://localhost:5173/github/callback
